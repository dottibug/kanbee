import { StyleSheet } from 'react-native';
import { colors } from './baseValues';

export const baseStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.lightGray,
  },
});
