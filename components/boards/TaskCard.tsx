import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

export default function TaskCard() {
  return (
    <SafeAreaView>
      <Text>Column Card</Text>
    </SafeAreaView>
  );
}
